/// Default indent size
const defaultIndent = 2;

/// XML attribute prefix in JSON by default
const defaultAttrPrefix = '@';

/// Text property name in JSON by default
const defaultTextNode = '#text';

/// CDATA property name in JSON by default
const defaultCdataNode = '#cdata';
